# ESP32-S3 PhotoPainter Partition Table
# Physical flash is 16 MB (verified on the target with `esptool flash_id`).
# nvs keeps its original offset and size so the provisioned HA device key,
# the WiFi credentials and the durable display state survive a repartition.
# Name,     Type, SubType,   Offset,   Size, Flags
nvs,        data, nvs,       0x9000,   0x6000,
phy_init,   data, phy,       0xf000,   0x1000,
factory,    app,  factory,   0x10000,  0x300000,
photocache, data, undefined, 0x310000, 0x100000,
